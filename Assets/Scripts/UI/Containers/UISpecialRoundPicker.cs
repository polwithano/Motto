using UI.Containers.Core;

namespace UI.Containers
{
    public class UISpecialRoundPicker : UIContainer
    {
        protected override void Open()
        {
            base.Open();
        }

        protected override void Close()
        {
            base.Close();
        }
    }
}
